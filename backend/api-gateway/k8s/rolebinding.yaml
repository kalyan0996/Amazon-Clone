apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: api-gateway
subjects:
  - kind: ServiceAccount
    name: api-gateway
roleRef:
  kind: Role
  name: api-gateway
  apiGroup: rbac.authorization.k8s.io
