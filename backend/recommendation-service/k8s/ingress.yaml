apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: recommendation-service
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  ingressClassName: nginx
  rules:
    - host: recommendation-service.amazon-clone.local
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: recommendation-service
                port:
                  number: 80
