apiVersion: apps/v1
kind: Deployment
metadata:
  name: recommendation-service
  labels:
    app: recommendation-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: recommendation-service
  template:
    metadata:
      labels:
        app: recommendation-service
    spec:
      serviceAccountName: recommendation-service
      containers:
        - name: recommendation-service
          image: amazon-clone/recommendation-service:latest
          ports:
            - containerPort: 8012
          envFrom:
            - configMapRef:
                name: recommendation-service-config
            - secretRef:
                name: recommendation-service-secret
          readinessProbe:
            httpGet:
              path: /readyz/
              port: 8012
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /healthz/
              port: 8012
            initialDelaySeconds: 10
            periodSeconds: 15
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 512Mi
