apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: wishlist-service
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  ingressClassName: nginx
  rules:
    - host: wishlist-service.amazon-clone.local
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: wishlist-service
                port:
                  number: 80
