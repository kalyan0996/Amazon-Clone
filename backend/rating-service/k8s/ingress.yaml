apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: rating-service
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  ingressClassName: nginx
  rules:
    - host: rating-service.amazon-clone.local
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: rating-service
                port:
                  number: 80
