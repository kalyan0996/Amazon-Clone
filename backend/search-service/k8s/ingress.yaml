apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: search-service
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  ingressClassName: nginx
  rules:
    - host: search-service.amazon-clone.local
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: search-service
                port:
                  number: 80
