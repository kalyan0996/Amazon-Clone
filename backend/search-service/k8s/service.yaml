apiVersion: v1
kind: Service
metadata:
  name: search-service
  labels:
    app: search-service
spec:
  selector:
    app: search-service
  ports:
    - port: 80
      targetPort: 8013
      protocol: TCP
  type: ClusterIP
