apiVersion: v1
kind: Service
metadata:
  name: analytics-service
  labels:
    app: analytics-service
spec:
  selector:
    app: analytics-service
  ports:
    - port: 80
      targetPort: 8018
      protocol: TCP
  type: ClusterIP
