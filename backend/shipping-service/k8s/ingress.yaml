apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: shipping-service
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  ingressClassName: nginx
  rules:
    - host: shipping-service.amazon-clone.local
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: shipping-service
                port:
                  number: 80
