apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: notification-service
spec:
  podSelector:
    matchLabels:
      app: notification-service
  policyTypes:
    - Ingress
    - Egress
  ingress:
    - from:
        - namespaceSelector:
            matchLabels:
              name: amazon-clone
  egress:
    - to:
        - namespaceSelector:
            matchLabels:
              name: amazon-clone
    - to:
        - namespaceSelector: {}
      ports:
        - protocol: UDP
          port: 53
