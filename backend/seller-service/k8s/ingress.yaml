apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: seller-service
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  ingressClassName: nginx
  rules:
    - host: seller-service.amazon-clone.local
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: seller-service
                port:
                  number: 80
