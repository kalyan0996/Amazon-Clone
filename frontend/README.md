# Frontend (Next.js)

Storefront UI for the amazon-clone platform, talking to the backend through the API gateway.

## Run locally

```bash
cp .env.example .env.local
npm install
npm run dev
```

Visit http://localhost:3000.

## Build

```bash
npm run build
npm start
```
